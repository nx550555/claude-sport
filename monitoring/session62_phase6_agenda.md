# Session_62 フェーズ6 統合動作確認 / A-3 遡及判断 議題リスト

> **目的**: フェーズ6 (統合動作確認) または A-3 遡及判断タスク実施時に参照する議題集積ファイル。
> **記録方針**: 各フェーズ進行中に発生した「次フェーズ以降で検討すべき論点」を時系列で蓄積する。
> **本ファイルは実装変更を伴わない議題記録専用**。実装は議題承認後に別タスクで実施する。

---

## 議題1: evidence の時間的独立性 (Session_62 フェーズ2 完了時 追加)

**追加日**: 2026-04-27 (フェーズ2 完了時、ユーザー指示により追加記録)

**論点**:
R024 (form slump 補正) の実装プロセス問題について、Session_61 handoff で指摘された「同一 turn 内 evidence 3/3 → implement」だけでなく、**evidence 3件目 (A041 Baptiste-Paolini) 自体が同一 turn 内の「ついで検出」であった**点も新プロトコル違反に該当する。

**具体的経緯**:
- evidence 1件目: A001 Lys-Badosa (旧セッション)
- evidence 2件目: A007 Sonmez-Paolini (旧セッション)
- evidence 3件目: A041 Baptiste-Paolini (Session_61 で Phase2 scope外UPSET スキャン中に検出)
- 同 Session_61 の同一 turn 内で evidence 3/3 到達 → R024 implement

→ evidence 3件目自体がユーザー質問契機の「ついで検出」だったため、(a) 同一 turn 内 implement、(b) 同一 turn 内 evidence 検出、の二重の時間的依存が発生していた。

**新プロトコルへの追加検討事項**:
`forbidden_practices` または `approval_workflow.step1_threshold_reached` に以下のチェックを加えるべきか検討:

- 「evidence 3件は時間的に独立した観察として蓄積されているか」の確認
- 具体判定基準案:
  - (案A) evidence 3件が **異なるセッション** での独立検出であること
  - (案B) evidence 3件が **異なる試合日** (例: 連続する3試合日以外) での検出であること
  - (案C) 同一セッション内で複数件 evidence が追加された場合は、最後の追加から **次セッション以降に proposal 生成** を必須化
  - (案D) ユーザー質問契機での scope外 UPSET 検出 evidence は、自発検出 evidence とは別カウントとして扱う

**判断必要時期**: フェーズ6 統合動作確認時、または A-3 遡及判断 (R024 取り消し / 承認 / 修正後再構築) 時。

**現時点のステータス**: **フェーズ6 第3段階で確定 (2026-04-28)**。

**確定方針**: **案C + 案A 併用** — 異セッション独立検出を原則 + proposal は次セッション以降。
- 同セッション内で複数件 evidence が追加された場合は、最後の追加から **次セッション以降** に proposal 生成を必須化 (案C)
- evidence 3件は原則として **異なるセッション** での独立検出であること (案A)
- ユーザー質問契機 evidence の別カウント (案D) は不採用 (検出契機による区別はせず、独立性のみで判定)
- **実装箇所**: 柱A `approval_workflow.step1_threshold_reached` 拡張 + `forbidden_practices` 追加項目 (同一 turn 内 evidence 検出 → implement の禁止) + CLAUDE.md【ルール改訂統制プロトコル】サブセクション3「新規ルール実装プロセス」に「evidence 時間的独立性チェック」追記

---

## 議題2: STEP 0.5 実施を毎回確実に保証する仕組み (Session_62 フェーズ3 完了時 追加)

**追加日**: 2026-04-27 (フェーズ3 完了時、ユーザー指示により追加記録)

**論点**:
CLAUDE.md 新設【結果反映 STEP 0.5・毎回必須】scope外 UPSET スキャン SOP のサブセクション6 (Session_61 逸脱パターンの再発防止) で定義した検証方法は、現時点では以下のみ:

> 「結果反映 commit と同時に『STEP 0.5 実施済』ログを `monitoring/pending_actions.md` または記録 commit message に明示」

この検証方法は **弱い**。理由:
- commit message は人間が事後で読み返すものであり、Claude が毎回必ず明示するという保証がない (CLAUDE.md 規定の遵守は Claude の自律性に依存)
- 過去 Session_30〜43 で multi_bets.json 更新が抜け続けた事故 (Session_44 で発覚) と同じ構造的リスク
- ユーザー側で「STEP 0.5 を実施したかどうか」を後から検証する手段が乏しい

**論点に含めるべき検討事項**:

フェーズ6 統合動作確認時に、以下のいずれか (または組合せ) を検討すべきか議題化:

- **(案A) 機械的チェッカー導入**: `scripts/verify_step05_executed.py` を作成し、結果反映 commit に対し直前 N時間以内の STEP 0.5 実行ログがあるか自動検証。`monitoring/health_check.py` から呼び出して WARN/ALERT 化
- **(案B) 構造化ログファイル新設**: `monitoring/step05_log.jsonl` を新設し、STEP 0.5 実行のたびに JSON Lines 形式で「実行日時 / 対象スポーツ / 検出件数 / ユーザー判断結果」を append。pending_actions.md 任意記述ではなく構造化された強制ログ
- **(案C) commit hook / pre-push hook**: 結果反映を含む commit に対し、commit message に `[STEP05:DONE]` または `[STEP05:N/A]` タグが含まれていることを必須化する Git hook を導入
- **(案D) records JSON の prediction_hit 更新時にフィールド追加**: `prediction_hit` を `true`/`false` 更新する際に、同じ entry に `step05_scanned_at` フィールドを必須化。未付与の prediction_hit 更新は health_check で検知
- **(案E) 出力フォーマット強制**: 結果反映タスクの最終報告フォーマットに「STEP 0.5 実施結果: N件検出 / 0件 (該当なし)」の行を必須化し、抜けたら自己チェック (CHECK-4) で気付ける構造

**判断必要時期**: フェーズ6 統合動作確認時。フェーズ4-5 完了後にユーザーと協議して、上記案 (またはその他案) のいずれを採用するか確定する。

**現時点のステータス**: **フェーズ6 第3段階で確定 (2026-04-28)**。

**確定方針**: **案A + 案D 併用** — 機械チェッカー + records フィールド。
- records エントリに `step05_scanned_at` フィールドを必須付与 (prediction_hit 確定時に同時付与) (案D)
- `monitoring/health_check.py` に **13項目目** として STEP 0.5 実施確認検査を追加 (案A 相当の機械チェッカー)
- 結果反映 commit に対し、対象試合 records の `step05_scanned_at` 未付与を WARN/ALERT 化
- (案B) 構造化ログ / (案C) commit hook / (案E) 出力フォーマット強制 は不採用 (案A+D で十分担保可能)
- **実装箇所**:
  - `monitoring/health_check.py` 13項目目として STEP 0.5 実施確認 (records JSON 走査 + step05_scanned_at 検出)
  - 柱B 結果反映 STEP 0.5 SOP に「step05_scanned_at 必須付与」追記
  - CLAUDE.md【結果反映 STEP 0.5・毎回必須】サブセクション2「スキャン実行手順」に step05_scanned_at 付与タイミング明示

---

## 議題3: confidence_drift 種目別閾値の確定 (Session_62 フェーズ4 ステージ1 完了時 追加)

**追加日**: 2026-04-27 (フェーズ4 ステージ1 完了時、4-1 MISS 重要度3段階分類 実装に伴い記録)

**論点**:
CLAUDE.md【柱C】4-1 セクションで新設した `confidence_drift` フラグについて、「推定勝率 ≥ 80% で予想したのに、実際は接戦/僅差勝利だった場合」に `"high"` を付与する規定を導入したが、各種目の「接戦/僅差」の機械判定基準は **TBD** として本フェーズでは未確定。

`confidence_drift=high` は Class B 相当 (中重要度) の深掘り対象となるため、種目別の薄勝ち閾値を曖昧なままにすると判定がブレる。フェーズ6 統合動作確認時に各種目の数値基準を確定する必要がある。

**各種目の薄勝ち基準候補 (フェーズ6 で確定する案)**:

| 種目 | 薄勝ち基準候補 | 補足 |
|---|---|---|
| **テニス (ATP/WTA)** | 3セット縺れ (2-1) で勝利 / または fav が 1セットでも落とした | tiebreak 含む 3-set go to distance / オッズ 1.30 以下の strong fav が 7-6, 6-1 等の 3-tiebreak 級含むか議論要 |
| **NHL** | OT/SO 勝利 / または regulation で1点差 (空ネット除く) | 終盤逆転 / 空ネットゴール (ENG) 加点後の最終スコアではなく試合中の差分で判定 |
| **NBA** | 1桁差 (9点差以下) で勝利 / または 4Q ガベージ前の差が 1桁 | プレーオフではより厳格 (5点差以下) 案も検討 |
| **MLB** | 1点差勝利 / または 9回裏まで同点進行で勝利 | walk-off / 延長戦勝利は薄勝ち扱い |
| **サッカー (5大リーグ)** | 1点差勝利 / または 後半AT で勝ち越し | 90分終了時 1点差で複数 chances 与えた xG 拮抗試合 |
| **NFL** | TD1個差以内 (8点差以下) で勝利 / または 4Q 残数分で同点進行 | covered the spread (-3, -7 等) で薄勝ちとなった場合別途追跡 |
| **NRL / Super Rugby / Premiership / Top14 / Pro D2 / Super League** | TD1個差以内 (7点差以下) で勝利 / または FT 直前まで競った | リーグ系 (NRL/Super League) と ユニオン系 (Super Rugby/Premiership/Top14/Pro D2) で得点感覚が異なるため別閾値検討要 |
| **MMA / その他追加スポーツ** | (該当スポーツ追加時に協議) | 現状システムに含まれず |

**判断必要時期**: フェーズ6 統合動作確認時。フェーズ4 ステージ2-3 / フェーズ5 完了後に確定する。

**現時点のステータス**: **フェーズ6 第3段階で確定 (2026-04-28)**。

**確定方針**: **Claude Code 提示の暫定案を採用**。種目別薄勝ち閾値は以下:

| 種目 | 薄勝ち閾値 (confidence_drift=high 付与基準) |
|---|---|
| **ATP / WTA テニス** | **3セット縺れ** (2-1 で勝利) |
| **NHL** | **OT 突入 OR regulation 1点差** |
| **NBA** | **1桁差** (1〜9点差) |
| **MLB** | **1点差** |
| **サッカー (5大リーグ)** | **1点差** |
| **NFL** | **TD 1個差以内** (1〜8点差) |
| **ラグビー (Premiership / Top14 / Pro D2 / NRL / Super League / Super Rugby)** | **TD 1個差以内** (リーグ系=6点差以内 / ユニオン系=7点差以内) |

- 推定勝率 ≥ 80% で予想 + 上記閾値内で勝利 → `confidence_drift: "high"` 付与必須
- フェーズ6 第3段階確定をもって `confidence_drift` フィールド付与は **必須化** (フェーズ4 ステージ1 「枠組みのみ」状態を解除)
- **実装箇所**: 柱C 4-1 セクションに「confidence_drift 種目別閾値テーブル」追加 + 「具体閾値確定までは任意」の文言を「フェーズ6 第3段階確定をもって必須化」に置換

---

## 議題4: タグ付与の粒度ガイドライン (Session_62 フェーズ4 ステージ3 完了時 追加)

**追加日**: 2026-04-27 (フェーズ4 ステージ3 完了時、4-3 情報源タグ義務化 実装に伴い記録)

**論点**:
CLAUDE.md【柱C】4-3 セクションで「タグは **主張ごと** に付与する」「1主張 = 1事実言明 or 1推論結論」「複数主張を含む段落は各主張の末尾にタグを付与」「句読点単位ではなく『論理的に独立した主張』単位で区切る」と規定したが、「論理的に独立した主張」の判定基準は記述者によってブレる可能性がある。同じ記述を複数の Claude セッションで再生成したとき、タグ付与単位が揺れて health_check の `miss_analysis_tag_compliance` 判定や CHECK-2 自己点検の結果が一貫しない懸念がある。

**具体的な解釈余地の例**:

```
Khachanov 6-2 6-3 で勝利 [FETCH:https://atptour.com/...]。Walton はクレー試合数 8試合のみ [FETCHER:tennis_elo]。
```

この記述を以下のいずれと解釈すべきかが現時点で曖昧:

- **解釈 A: 2主張**として扱う
  - 主張1: 「Khachanov 6-2 6-3 で勝利」(試合結果の事実) → `[FETCH:URL]`
  - 主張2: 「Walton はクレー試合数 8試合のみ」(選手スタッツの事実) → `[FETCHER:tennis_elo]`
  - 各主張の末尾にタグが付与されており、現行運用通り
- **解釈 B: 1主張のデータ列挙**として扱う
  - 「Khachanov の勝利 + Walton のクレー試合数」を一連の事実セットと見る
  - この場合タグは段落末1つで足りる、または各データ点に付くタグは「列挙の補足記号」扱い

解釈 A vs B のどちらを採用するかで以下が変わる:
- health_check の文単位タグ検出ロジック (`re.split(r"[。\n]+", ma)` で分解後の文ごと判定) の妥当性
- CHECK-2 自己点検時の「主張 1個に対しタグ 1個」要件の厳格度
- 文末タグなしの短い接続句 (例:「なお、」「ただし、」) の扱い

**追加で曖昧な境界例**:

- 推論連鎖: 「A [FETCH] のため B が起こり、C が想定される [INFER]」 → 「B が起こり」部分は中間推論で独立タグ要否?
- 否定主張: 「X ではない [FETCH:URL]」 → 否定言明も独立主張として扱うか?
- 程度副詞付き事実: 「serve in% は **やや低かった** 52% [FETCHER:tennis_serve]」 → 「やや低かった」評価語と「52%」数値は別主張か?
- 複合主語: 「A と B はともに敗退した [FETCH:URL]」 → 複数の対象に同じ事実が適用される場合、1主張か複数主張か?

**判断必要時期**: フェーズ6 統合動作確認時。粒度ガイドライン (例: 「動詞単位で区切る」「主語が変わったら必ず別主張」「数値+評価語は1主張」等) を協議し確定する。

**現時点のステータス**: **フェーズ6 第3段階で確定 (2026-04-28)**。

**確定方針**: **解釈B + 但し書き**。

ガイドライン確定内容:
1. **同一 source からの複数データ列挙は段落末1タグ可** (解釈B 採用)
   - 例: `Khachanov 6-2 6-3 で勝利、1st serve in% 72% [FETCH:URL]` → 同一試合レポートからの抽出のため段落末1タグ可
2. **source が異なる主張は別タグ必須** (但し書き①)
   - 例: `Khachanov 6-2 6-3 で勝利 [FETCH:atp_url]。Walton クレー 8試合のみ [FETCHER:tennis_elo]` → source 別のため両タグ必須
3. **INFER と FETCH/FETCHER/SEARCH の混在文は両方タグ必須** (但し書き②)
   - 例: `xG はホーム 2.1 / アウェー 0.7 [FETCHER:understat]。→ ホーム決定力差で接戦化 [INFER]` → 事実 + 推論の両方にタグ必須
4. **否定主張も事実に基づく場合はタグ必須** (但し書き③)
   - 例: `Sinner は怪我なしで出場可能 [FETCH:URL]` → 否定形でも事実主張のためタグ必須
5. **INFER の推論連鎖は末尾1タグでOK** (但し書き④)
   - 例: `→ サーブ依存型 player の rhythm が標高で崩れた可能性 [INFER]` → 中間推論にタグなしでも末尾1タグで可

- **実装箇所**: 柱C 4-3 セクションに「タグ付与の粒度ガイドライン」サブセクション追加 + `memory/feedback_no_fabrication.md` 補強 (但し書き5項目を追記)
- health_check の文単位タグ検出ロジックは現行通り維持 (短い接続句 10文字未満 はカウント対象外も維持)

---

## 議題5: match_status 遡及範囲 (Session_62 フェーズ5 ステップ4 完了時 追加)

**追加日**: 2026-04-27 (フェーズ5 ステップ4 完了時、柱D サブセクション2 match_status enum 仕様 実装に伴い記録)

**論点**:
柱D サブセクション2 で導入した `match_status` enum (5値: completed / retired / walkover / cancelled / postponed) について、既存 records/*.json (100件超) への遡及付与を実施するか、新規エントリのみ付与で運用するかが未確定。柱D サブセクション3 では「新規エントリのみ。遡及付与なし」と規定しているが、既存の `void: true` フィールドとの整合性を実運用で取るには遡及方針を明確化する必要がある。

**詳細検討事項**:

- **(案A) 既存 `void: true` を `cancelled` 扱いとして遡及付与する**
  - メリット: void フラグと match_status の両方が整合した状態となり、health_check 等の検査ロジックが一貫する
  - デメリット: 既存 void:true には実際は retired / walkover / cancelled / postponed が混在している可能性があり、cancelled 一律付与は事実と異なる
- **(案B) retired / walkover を区別する判定ロジックを実装して遡及付与する**
  - メリット: 事実に即した match_status が付与される
  - デメリット: 各 records エントリの WebSearch 再確認が必要 (100件超) で実装コスト高
- **(案C) 新規のみで運用し、既存は `void: true` のまま放置する** (柱D サブセクション3 の現行規定通り)
  - メリット: 実装コストゼロ・遡及不要
  - デメリット: 既存と新規でフィールド整合性が分断され、cumulative.json 集計や by_match_status 集計時に分岐ロジック必要

**判断必要時期**: フェーズ6 統合動作確認時。

**現時点のステータス**: **フェーズ6 第3段階で確定 (2026-04-28)**。

**確定方針**: **案B 採用** — retired/walkover 区別判定して遡及付与。
- 既存 records 100件超の `void: true` エントリを WebSearch で再確認し、`match_status` を `retired` / `walkover` / `cancelled` / `postponed` のいずれかに判定
- 既存 `void: true` フィールドは後方互換用として維持 (削除せず・両方併存)
- **実装は4本柱完了後の遡及判断タスクと統合実施** (Session_61 由来 12件 + 9件振り分け + R024→R025 再付番タスク等と一括処理)
- **フェーズ6 第4段階では agenda.md 記録のみ・実装変更なし** (柱D サブセクション3 の現行規定「新規のみ・遡及なし」も「フェーズ6 では維持・別タスクで遡及付与実施」と但し書き追加)
- **実装箇所 (別タスク)**: `records/{sport}/*.json` 全件走査 + WebSearch 再確認 + match_status 付与スクリプト (`scripts/_retroactive_match_status.py` 等の使い捨てスクリプト想定)

---

## 議題6: 大会優先度テーブル維持責任 (Session_62 フェーズ5 ステップ4 完了時 追加)

**追加日**: 2026-04-27 (フェーズ5 ステップ4 完了時、柱D サブセクション4 大会優先度テーブル + サブセクション6 新リーグ追加 SOP 実装に伴い記録)

**論点**:
柱D サブセクション6「新リーグ追加 SOP」では、新リーグ追加時に CLAUDE.md 大会優先度テーブルを「柱A 適用対象外、追記のみ」と記載した。一方、ルール改訂統制プロトコル (柱A) では CLAUDE.md 改訂全般を B区分 (承認必須) として規定しており、新リーグ追加時のテーブル更新が両規定に対し矛盾する可能性がある。

**詳細検討事項**:

- **矛盾の構造**:
  - 柱A サブセクション1: CLAUDE.md / `core/framework.json` / `core/rules_*.json` / `core/rule_pipeline.json` の status 変更 = B区分 (承認必須)
  - 柱D サブセクション6: 新リーグ追加時の大会優先度テーブル更新 = 「柱A 適用対象外、追記のみ」
  - → CLAUDE.md 改訂は B区分のため、サブセクション6 の規定が柱A と整合しない可能性
- **(案A) 大会優先度テーブル更新も B区分 (承認必須) として運用**
  - メリット: 柱A との整合性確保・新リーグ追加時の判定ロジック影響をユーザー承認可能
  - デメリット: 新リーグ追加のたびに承認プロセス必須となり運用負荷増
- **(案B) 大会優先度テーブル「追記のみ」は柱A 適用対象外として明示的に例外規定化**
  - メリット: 新リーグ追加の運用負荷軽減
  - デメリット: 柱A の包括規定からの除外項目を増やすことで承認制プロセスが形骸化するリスク
- **(案C) 既存大会の名称変更 (例: ATP 250 → ATP Tour 250) は B区分・新リーグ追加は柱A 適用対象外と階層化**
  - メリット: 名称変更 (判定ロジックに影響) は承認・新規追加 (追加のみで既存ロジック非影響) は承認不要と論理整理
  - デメリット: 名称変更と新リーグ追加の境界判断にケースバイケース対応が必要

**判断必要時期**: フェーズ6 統合動作確認時。

**現時点のステータス**: **フェーズ6 第3段階で確定 (2026-04-28)**。

**確定方針**: **案C 採用** — 名称変更=B区分 + 新リーグ追加=対象外 階層化。
- **既存大会の名称変更** (例: ATP 250 → ATP Tour 250 / Premiership → Gallagher Premiership 等) → **B区分 (柱A 承認必須)**
  - 理由: 名称変更は判定ロジック (rules_*.json 内の大会名照合等) に影響するため
- **新リーグ追加** (例: 新スポーツ・新リーグの scope 追加) → **柱A 適用対象外、追記のみ**
  - 理由: 既存ロジック非影響・追加のみのため運用負荷軽減
- 名称変更 / 新リーグ追加の境界判断は **ケースバイケース対応** (リーグ統合・分割等の中間ケースは個別判断)
- **実装箇所**: 
  - 柱D サブセクション6「新リーグ追加 SOP」の文言修正 (「柱A 適用対象外、追記のみ」→「新リーグ追加=柱A 適用対象外・名称変更=B区分・両者の境界判断はケースバイケース」)
  - 柱A サブセクション1「適用対象 (承認必須ファイル)」に「CLAUDE.md 既存大会の名称変更は B区分 / 新リーグ追加は対象外」の階層化規定追記

---

## 議題7: 区分2 (reference_only) cumulative 集計除外境界 (Session_62 フェーズ5 ステップ4 完了時 追加)

**追加日**: 2026-04-27 (フェーズ5 ステップ4 完了時、柱D サブセクション1 3区分定義 実装に伴い記録)

**論点**:
柱D サブセクション1 で「区分2 (reference_only) は cumulative.json 集計対象外」と定義したが、v3.0 2トラック精度管理 (Track 1 = ベット推奨収益性 / Track 2 = 全試合モデル品質) のうち Track 2 では区分2 も含めるべきかが議論余地あり。

**詳細検討事項**:

- **Track 1 (ベット推奨収益性)**: 区分1 (full_record) のみ寄与で確定 (柱D サブセクション1・柱C サブセクション4-1 と整合)
- **Track 2 (全試合モデル品質)**: 区分2 を含めるか / 区分1 のみとするか未確定
  - **(案A) Track 2 も区分1 のみ集計** (現行柱D 規定通り)
    - メリット: シンプル・cumulative.json 集計ロジック単純化
    - デメリット: scope外UPSET スキャン (柱B) でユーザー (B) 承認した試合の精度追跡が cumulative に反映されず、Track 2 の網羅性が低下
  - **(案B) Track 2 は区分1 + 区分2 を集計**
    - メリット: 全試合モデル品質を網羅的に追跡
    - デメリット: cumulative.json に `by_record_class` セクション追加が必要・既存集計ロジックの改修
  - **(案C) `by_record_class` 集計セクションを cumulative.json に追加し、区分1 / 区分2 を別軸で集計**
    - メリット: Track 1 (区分1 のみ) と Track 2 拡張版 (区分1+2) を両方追跡可能
    - デメリット: dashboard.html 表示拡張も同期して必要
- **議題3 (confidence_drift 種目別閾値) との連動**:
  - confidence_drift=high HIT は柱C サブセクション4-1 で Class B 相当として深掘り対象に位置付けられている
  - 区分2 試合の confidence_drift=high HIT も Track 2 集計対象とするかは議題3 確定と同時検討が整合的
  - フェーズ6 で議題3 と議題7 を併せて協議する案

**判断必要時期**: フェーズ6 統合動作確認時 (議題3 と同時)。

**現時点のステータス**: **フェーズ6 第3段階で確定 (2026-04-28)**。

**確定方針**: **案C 採用** — `by_record_class` 別軸集計。
- **Track 1 (ベット推奨収益性)** = **区分1 のみ** (現行維持・収益性追跡)
- **Track 2 (全試合モデル品質)** = **`by_record_class` 別軸集計** (区分1 のみ + 区分1+2 両方の数値を並列追跡)
- cumulative.json に `by_record_class` セクション新設:
  - `record_class_1_only`: 区分1 のみの hit_rate / EV / sport別 / quadrant別
  - `record_class_1_and_2`: 区分1+2 合算の hit_rate / EV / sport別 / quadrant別
- dashboard.html 表示拡張も同期して必要 (Track 2 の二軸表示)
- **cumulative.json 改修・dashboard.html 改修は4本柱完了後の別タスク** (Session_61 由来 12件凍結解除と並行 / cumulative_history.json 連動も同タスクで実施)
- **フェーズ6 第4段階では agenda.md 記録のみ・実装変更なし**
- **実装箇所**:
  - 柱D サブセクション1「3区分定義」の cumulative 寄与記述を「区分2 は Track 1 集計対象外、Track 2 では `by_record_class` 別軸集計対象」に改訂
  - 柱C サブセクション v3.0 2トラック精度管理整合表を更新 (区分2 の Track 2 寄与を「○ (by_record_class 別軸)」に)

---

## 議題8: 境界曖昧リーグ判断踏襲の有効期限 (Session_62 フェーズ5 ステップ4 完了時 追加)

**追加日**: 2026-04-27 (フェーズ5 ステップ4 完了時、柱D サブセクション8 境界曖昧リーグ個別判断 SOP 実装に伴い記録)

**論点**:
柱D サブセクション8 で「同一リーグの今後の試合は前回判断踏襲 (ユーザー再判断不要)」と規定したが、シーズン跨ぎや大会フォーマット変更時に再判断が必要となる可能性があり、踏襲の有効期限ルールが未確定。

**詳細検討事項**:

- **シーズン跨ぎでの再判断**:
  - (例) ATP 500 R1 を Session_X で「区分2」と判断 → ATP 2025-2026 シーズン終了後の 2026-2027 シーズン開幕時に踏襲継続するか / 再判断するか
  - シーズンごとに seeded chain 構造・bye ルール・出場選手層が変動するため、踏襲の有効性は無期限ではない可能性
- **リーグ内 round 階層ごとの踏襲範囲**:
  - (例) ATP 500 R1 の判断 (区分2) と R2 の判断 (区分1) は連動するか / 独立か
  - WTA 125K の R1 / R2 / R3 / SF / F で踏襲範囲をどこまで広げるか
- **大会フォーマット変更時の扱い**:
  - (例) ATP 250 が新規 250 シリーズに格上げされた場合、過去の「区分3」判断は無効化されるか
  - リーグ統合・分割 (例: Super League の structure 変更) 時の踏襲継続性
- **(案A) シーズン単位で踏襲リセット (シーズン跨ぎで毎回再判断)**
  - メリット: シーズンごとのリーグ実情を反映可能
  - デメリット: 運用負荷高
- **(案B) フォーマット変更時のみ再判断 (シーズン継続中は踏襲)**
  - メリット: 運用負荷軽減
  - デメリット: 同一フォーマットでも seeded chain 等の変動は反映されない
- **(案C) round 階層ごとに踏襲粒度を分離 (R1 と R2 以降を別判断)**
  - メリット: テニス R1 bye の特殊性に対応可能
  - デメリット: 踏襲ルールが複雑化

**判断必要時期**: フェーズ6 統合動作確認時。

**現時点のステータス**: **フェーズ6 第3段階で確定 (2026-04-28)**。

**確定方針**: **案B 原則 + シーズン跨ぎ見直し提案 + フォーマット変更時強制再判断**。
- **同一フォーマット継続中** は前回判断を踏襲 (案B 原則)
- **シーズン跨ぎ時** は Claude Code が **見直し提案を1回** 行う (踏襲継続 / 再判断 をユーザーに問う)
- **大会フォーマット変更時** (リーグ統合・分割・round 構造変更等) は **強制再判断** (Claude 自発判断禁止・ユーザー判断必須)
- **シーズン跨ぎ判定基準 (種目別)**:

| 種目 | シーズン跨ぎ判定基準 |
|---|---|
| **ATP / WTA テニス** | ATP/WTA Tour カレンダー (1月〜11月) で年度跨ぎ (毎年 11月→1月の Off-season 切れ目) |
| **NHL / NBA / MLB / NFL (北米プロ)** | 各リーグのレギュラーシーズン開幕日 (NHL=10月 / NBA=10月 / MLB=3-4月 / NFL=9月) |
| **サッカー (5大リーグ)** | ヨーロッパシーズン (8月〜5月) で年度跨ぎ (毎年 5月→8月の Off-season 切れ目) |
| **ラグビー (Premiership / Top14 / Pro D2 / NRL / Super Rugby / Super League)** | 各リーグの公式シーズン開幕日 |
| **CFL / UFL / AHL** | 各リーグのレギュラーシーズン開幕日 (CFL=6月 / UFL=3月 / AHL=10月) |

- **実装箇所**: 柱D サブセクション8「境界曖昧リーグ個別判断 SOP」に「踏襲有効期限」サブセクション追加 (シーズン跨ぎ判定基準テーブル + フォーマット変更時の強制再判断ルール)

---

## 議題9: ルール提案レポート (d) シミュレーション計算ロジック (Session_62 フェーズ6 第1段階完了時 追加)

**追加日**: 2026-04-27 (フェーズ6 第1段階 仮想 Session_63 ワークフロー書き出し完了時、ユーザー指示により追加記録)

**論点**:
柱A 【ルール改訂統制プロトコル】3. 新規ルール実装プロセス で規定した提案レポート必須項目 (d)「承認時の cumulative.json への影響シミュレーション (過去予測への遡及適用で hit_rate / EV がどう変動するか)」について、実運用で Claude Code が cumulative.json から自動計算する具体的ロジックが未整備。

**詳細検討事項**:

- **過去 records から該当条件試合を抽出するロジック**:
  - 候補ルールの適用条件 (例: P031 の「Bundesliga 12-16位帯 + home」) を機械判定可能な形で記述する標準フォーマット要否
  - records/{sport}/*.json を全件走査して条件マッチする試合を抽出するヘルパー関数 (`scripts/simulate_rule_impact.py` 等) の実装要否
  - 抽出条件が rule body の自然言語記述のみの場合、Claude が条件を都度解釈する運用負荷
- **仮にルール適用した場合の hit_rate / EV 再計算ロジック**:
  - 抽出した過去試合に対し、新ルール適用後の predicted_winner / confidence / EV を再計算
  - 再計算後の prediction_hit との突合せで hit_rate 変動を試算
  - cumulative.json の既存集計 (sport別 / quadrant別) との整合的な分岐
- **自動計算 vs 半自動 (Claude が手動計算してレポートに記載)**:
  - **(案A) 完全自動化**: `scripts/simulate_rule_impact.py` を新設し、候補ID + 適用条件 (構造化) を入力するとシミュレーション結果を出力。提案レポート (d) は自動生成
    - メリット: 計算精度・再現性確保 / Claude 判断のブレ排除
    - デメリット: 適用条件の構造化フォーマット標準化が必要 / スクリプト実装コスト
  - **(案B) 半自動 (Claude 手動計算 + memory ベースガイドライン)**:
    - メリット: 実装コスト低 / 候補ごとの適用条件の柔軟性確保
    - デメリット: Claude の都度判断でブレが発生する可能性 / 検証コスト高
  - **(案C) シミュレーション省略 + 影響範囲の定性記述のみ**:
    - メリット: 提案レポート生成負荷軽減
    - デメリット: 柱A 必須項目 (d) を実質骨抜きにする / 過去予測への遡及影響が見えないままユーザー判断を仰ぐリスク
- **適用対象 records 範囲の判断**:
  - 全 records (100件超) を対象とするか、最近 N試合に限定するか
  - 区分1 (full_record) のみ対象とするか、区分2 (reference_only) も含めるか (議題7 連動)

**判断必要時期**: フェーズ6 統合動作確認時、または 4本柱完了後の別タスク (柱A 運用初動段階で実装パターンが固まったタイミング)。

**現時点のステータス**: **フェーズ6 第3段階で確定 (2026-04-28)**。

**確定方針**: **案B 採用** — 半自動 (Claude 手動計算 + memory ガイドライン化)。
- フェーズ6 第3段階では **「案B 採用」を確定**
- **memory ガイドライン作成は4本柱完了後の遡及判断タスクと統合実施** (柱A 運用初動段階で実装パターンが固まったタイミングで作成)
- **フェーズ6 第4段階では agenda.md 記録のみ・実装変更なし**
- **ガイドライン記載予定内容 (別タスクで具体化)**:
  - 該当条件試合の抽出方法 (rule body の自然言語条件 → records JSON 走査クエリへの変換手順)
  - hit_rate / EV 再計算式 (新ルール適用後の predicted_winner / confidence / EV 再計算手順)
  - 計算根拠の記述義務 (検算可能な形での明示 — 抽出件数・該当試合 ID リスト・再計算結果の段階記述)
- **実装箇所 (別タスク)**: `memory/feedback_rule_simulation_guideline.md` 新規作成 (4本柱完了後の柱A 運用初動段階で具体化)

---

## (今後の議題追加スペース)

フェーズ6 進行中に発生した追加論点は、本ファイルに「議題10」「議題11」... として時系列追記する。

---

## 【フェーズ6 第3段階 確定サマリ】 (2026-04-28)

Session_62 フェーズ6 第3段階で議題1〜9 全件確定。確定内容を以下に一覧化する。

### 確定方針一覧表

| 議題 | 論点 | 確定方針 | 実装区分 | 実装ステータス |
|---|---|---|---|---|
| **議題1** | evidence の時間的独立性 | **案C + 案A 併用** (異セッション独立検出 + proposal は次セッション以降) | 第4段階で即時反映 (柱A) | **✅ 第4段階で実装反映完了 (2026-04-28 / commit db7e151)** |
| **議題2** | STEP 0.5 実施保証の仕組み | **案A + 案D 併用** (機械チェッカー + records `step05_scanned_at` フィールド) | 第4段階で即時反映 (柱B + health_check 13項目目) | **✅ 第4段階で実装反映完了 (2026-04-28 / commit 5ae443d + 2b205a8)** |
| **議題3** | confidence_drift 種目別閾値 | **Claude Code 暫定案採用** (テニス 3セット縺れ / NHL OT・1点差 / NBA 1桁差 / MLB 1点差 / サッカー 1点差 / NFL 8点差以下 / ラグビー TD1個差以内) | 第4段階で即時反映 (柱C 4-1) | **✅ 第4段階で実装反映完了 (2026-04-28 / commit 13f45b4)** |
| **議題4** | タグ付与の粒度ガイドライン | **解釈B + 但し書き5項目** (同一 source 段落末1タグ可 / source 別は別タグ / FACT+INFER 混在は両方 / 否定主張も必須 / INFER 連鎖は末尾1タグ可) | 第4段階で即時反映 (柱C 4-3 + memory) | **✅ 第4段階で実装反映完了 (2026-04-28 / commit d3c8720)** |
| **議題5** | match_status 遡及範囲 | **案B 採用 (retired/walkover 区別判定 + 遡及付与)** | 別タスク統合 (フェーズ6 では agenda.md 記録のみ) | 🔵 別タスク統合維持 (4本柱完了後の遡及判断タスクで実施) |
| **議題6** | 大会優先度テーブル維持責任 | **案C 採用** (名称変更=B区分 + 新リーグ追加=対象外 階層化) | 第4段階で即時反映 (柱D サブセクション6 + 柱A サブセクション1) | **✅ 第4段階で実装反映完了 (2026-04-28 / commit db7e151 + 00611cc)** |
| **議題7** | 区分2 cumulative 集計除外境界 | **案C 採用** (`by_record_class` 別軸集計 / Track 1=区分1 のみ / Track 2=区分1 のみ + 区分1+2 並列) | 文言反映=第4段階 / cumulative.json/dashboard.html 改修=別タスク統合 | **✅ 文言反映完了 (2026-04-28 / commit 00611cc)** + 🔵 cumulative/dashboard 改修は別タスク統合維持 |
| **議題8** | 境界曖昧リーグ判断踏襲の有効期限 | **案B 原則 + シーズン跨ぎ見直し提案 + フォーマット変更時強制再判断** (種目別シーズン跨ぎ判定基準テーブル付き) | 第4段階で即時反映 (柱D サブセクション8) | **✅ 第4段階で実装反映完了 (2026-04-28 / commit 00611cc)** |
| **議題9** | ルール提案レポート (d) シミュレーション計算ロジック | **案B 採用** (半自動 + memory ガイドライン化) | 別タスク統合 (memory ガイドライン作成は4本柱完了後・フェーズ6 では agenda.md 記録のみ) | 🔵 別タスク統合維持 (4本柱完了後の遡及判断タスクで実施) |

### 第4段階で即時反映する改修対象ファイル

| ファイル | 反映内容 |
|---|---|
| `CLAUDE.md`【ルール改訂統制プロトコル】(柱A) | 議題1 (evidence 時間的独立性) + 議題6 (名称変更 B区分 / 新リーグ追加対象外 階層化) |
| `CLAUDE.md`【結果反映 STEP 0.5】(柱B) | 議題2 (`step05_scanned_at` 必須付与) |
| `CLAUDE.md`【柱C】4-1 + 4-3 | 議題3 (confidence_drift 種目別閾値テーブル) + 議題4 (タグ付与の粒度ガイドライン サブセクション) |
| `CLAUDE.md`【柱D】サブセクション1, 6, 8 | 議題6 (新リーグ追加 SOP 文言修正) + 議題7 (区分2 集計記述改訂・実装は別タスク) + 議題8 (踏襲有効期限サブセクション追加) |
| `core/rule_pipeline.json` `instructions_for_claude` + `approval_workflow` + `forbidden_practices` | 議題1 (evidence 時間的独立性チェック追加) |
| `monitoring/health_check.py` | 13項目目 STEP 0.5 実施確認検査追加 (議題2) |
| `memory/feedback_no_fabrication.md` | 議題4 (タグ付与の粒度ガイドライン 但し書き5項目補強) |
| `monitoring/session62_phase6_agenda.md` (本ファイル) | 議題1〜9 確定方針反映済 (本コミットで完了) |

### 別タスク統合 (フェーズ6 では agenda.md 記録のみ・4本柱完了後の遡及判断タスクで実施)

| 議題 | 別タスク内容 |
|---|---|
| **議題5** | 既存 records 100件超の void:true エントリ → WebSearch 再確認 → match_status 遡及付与 |
| **議題7** | cumulative.json 改修 (`by_record_class` セクション新設) + dashboard.html 表示拡張 |
| **議題9** | `memory/feedback_rule_simulation_guideline.md` 新規作成 (シミュレーション計算ロジック半自動運用ガイドライン) |

### 第4段階の推奨実装順序

1. **柱A 改訂** (議題1 + 議題6 階層化) — `core/rule_pipeline.json` + CLAUDE.md
2. **柱B 改訂** (議題2 step05_scanned_at) — CLAUDE.md
3. **柱C 4-1 改訂** (議題3 confidence_drift 種目別閾値) — CLAUDE.md
4. **柱C 4-3 改訂** (議題4 粒度ガイドライン) — CLAUDE.md + `memory/feedback_no_fabrication.md`
5. **柱D 改訂** (議題6 + 議題7 文言 + 議題8) — CLAUDE.md
6. **health_check.py 13項目目追加** (議題2 機械チェッカー)
7. **agenda.md ステータス更新** (本コミットで実施済)
8. **commit + push** (柱単位で commit 分割)

### 保留中・別タスク継続対象

- Session_61 由来 12件 (Modified 8 + Untracked 4) は引き続き凍結維持
- ready_to_implement 候補 P020 / P024 凍結維持
- R017 v2.0 / R024 v1.0 / P030 ID衝突解消は4本柱完了後の遡及判断タスクで判定
- Session_61 9件振り分け実施は4本柱完了後の遡及判断タスクで実施
- 議題5 (match_status 遡及付与) / 議題7 (cumulative 改修) / 議題9 (memory ガイドライン) は4本柱完了後の遡及判断タスクと統合実施

---

## 【フェーズ6 第4段階 実装反映完了サマリ】 (2026-04-28)

Session_62 フェーズ6 第4段階で議題1〜9 のうち即時反映対象 (議題1/2/3/4/6/7文言/8) の実装反映を全件完了。各ステップで commit を分割 (柱単位) し、ステップ7 完了時に統合 push を実施。

### 第4段階 7 commit 構成

| 順序 | commit ID | message | 反映議題 | 改修ファイル |
|---|---|---|---|---|
| 1 | `db7e151` | Session_62 phase6 stage4 step1: 柱A 改訂 (議題1 evidence 時間的独立性 + 議題6 CLAUDE.md 階層化) | 議題1 + 議題6 | `core/rule_pipeline.json` + `CLAUDE.md` 柱A サブセクション1-1 + 3-1 |
| 2 | `5ae443d` | Session_62 phase6 stage4 step2: 柱B 改訂 (議題2 step05_scanned_at 規定 + 機械検証移行) | 議題2 規定部分 | `CLAUDE.md` 柱B サブセクション2 ステップ7 + サブセクション6 拡張 |
| 3 | `13f45b4` | Session_62 phase6 stage4 step3: 柱C 4-1 改訂 (議題3 confidence_drift 種目別閾値テーブル + 必須化) | 議題3 | `CLAUDE.md` 柱C 4-1 confidence_drift フラグ サブセクション 全面改訂 |
| 4 | `d3c8720` | Session_62 phase6 stage4 step4: 柱C 4-3 改訂 (議題4 タグ付与の粒度ガイドライン) | 議題4 | `CLAUDE.md` 柱C 4-3 タグ付与の粒度ガイドライン サブセクション 新設 + `memory/feedback_no_fabrication.md` (git 管轄外) + `memory/MEMORY.md` (git 管轄外) |
| 5 | `00611cc` | Session_62 phase6 stage4 step5: 柱D 改訂 (議題6 サブセクション6 3層構造 + 議題7 文言 + 議題8 踏襲有効期限) | 議題6 + 議題7 文言 + 議題8 | `CLAUDE.md` 柱D サブセクション1 整合表新設 + サブセクション6 全面改訂 + サブセクション8 踏襲有効期限新設 |
| 6 | `2b205a8` | Session_62 phase6 stage4 step6: monitoring/health_check.py 13項目目追加 (議題2 step05_scan_compliance 機械検証) | 議題2 機械チェッカー部分 | `monitoring/health_check.py` 13項目目 step05_scan_compliance 実装 |
| 7 | (本commit) | Session_62 phase6 stage4 step7: agenda.md 議題1〜9 第4段階実装反映完了ステータス更新 | 全議題 | `monitoring/session62_phase6_agenda.md` ステータス列追加 + 第4段階完了サマリ追記 |

### 第4段階で即時反映完了した議題サマリ

| 議題 | 改修内容サマリ |
|---|---|
| **議題1** (evidence 時間的独立性) | rule_pipeline.json: instructions_for_claude / approval_workflow.step1_threshold_reached / forbidden_practices に時間的独立性チェック追加。CLAUDE.md 柱A サブセクション3-1 で規定明文化 (異セッション独立 + 同一セッション内複数件追加時は次セッション以降 proposal / 同一 turn 内 evidence 3件目 → implement 完全禁止 / 案D 不採用) |
| **議題2** (STEP 0.5 実施保証) | CLAUDE.md 柱B サブセクション2 ステップ7 で `step05_scanned_at` フィールド必須付与規定 (ISO 8601 UTC タイムスタンプ / 検出 0件でも付与必須 / 遡及付与なし)。サブセクション6 で旧検証方法 (commit message) + 新検証方法 (records JSON フィールド機械検証) の二重担保。health_check.py 13項目目 step05_scan_compliance で機械チェック (完全未付与=ALERT / 部分未付与=WARN / 区分3 走査対象外) |
| **議題3** (confidence_drift 種目別閾値) | CLAUDE.md 柱C 4-1 confidence_drift フラグ サブセクション 全面改訂。判定対象明確化 (≥80% conf + HIT のみ / <80% / 敗戦 / 試合不成立 / 区分3 は対象外) + 種目別薄勝ち閾値テーブル新設 (7種目: テニス 3セット縺れ / NHL OT or 1点差 / NBA 1桁差 / MLB 1点差 / サッカー 1点差 / NFL TD1個差 / ラグビー TD1個差 リーグ系6点 ユニオン系7点) + 必須化文言置換 + JSON 記述例 |
| **議題4** (タグ付与の粒度ガイドライン) | CLAUDE.md 柱C 4-3 「タグ付与の粒度ガイドライン」サブセクション 新設 (解釈B + 但し書き4項目: source 別 / FACT+INFER 混在 / 否定主張 / INFER 連鎖 + 短い接続句のカウント対象外規定 + 解釈緩和の影響 + 既存サブセクションとの位置付け)。memory/feedback_no_fabrication.md + MEMORY.md (両方 git 管轄外) も同義の追記 + frontmatter 更新 |
| **議題6** (大会優先度テーブル維持責任) | CLAUDE.md 柱A サブセクション1-1 で CLAUDE.md 改訂の階層化規定 (B区分 名称変更 / 対象外 新リーグ追加 / 境界判断 ケースバイケース) + 柱D サブセクション6 全面改訂 (6-1 新リーグ追加ワークフロー / 6-2 既存大会の名称変更 / 6-3 境界判断) |
| **議題7 文言** (区分2 cumulative 集計除外境界) | CLAUDE.md 柱D サブセクション1 区分2 寄与記述改訂 ("cumulative.json 集計対象外" → "Track 1 集計対象外、Track 2 では `by_record_class` 別軸集計対象") + v3.0 2トラック精度管理整合表新設 (Track 1 = 区分1 のみ / Track 2 = `by_record_class` 別軸 = `record_class_1_only` + `record_class_1_and_2`)。**cumulative.json / dashboard.html 改修は別タスク継続** |
| **議題8** (境界曖昧リーグ判断踏襲の有効期限) | CLAUDE.md 柱D サブセクション8 「踏襲有効期限」サブセクション新設 (案B 原則 + シーズン跨ぎ見直し提案1回 + フォーマット変更時強制再判断 + 種目別シーズン跨ぎ判定基準テーブル5区分: テニス / 北米プロ / サッカー / ラグビー / CFL UFL AHL) |

### 別タスク統合議題の現状継続 (本フェーズで実装変更なし)

| 議題 | 別タスク内容 | 実施タイミング |
|---|---|---|
| **議題5** (match_status 遡及範囲) | 既存 records 100件超の void:true エントリ → WebSearch 再確認 → match_status 遡及付与 | 4本柱完了後の遡及判断タスクと統合実施 |
| **議題7 cumulative 改修部分** | cumulative.json `by_record_class` セクション新設 + dashboard.html 表示拡張 (Track 2 二軸表示) | 4本柱完了後の遡及判断タスクと統合実施 |
| **議題9** (ルール提案レポート (d) シミュレーション計算ロジック) | `memory/feedback_rule_simulation_guideline.md` 新規作成 (半自動運用ガイドライン) | 4本柱完了後の柱A 運用初動段階で具体化 |

### 凍結維持中の対象 (本フェーズで一切手を付けない)

- **Session_61 由来 12件 (Modified 8 + Untracked 4)**: `core/dashboard_stats.json` / `core/rules_tennis.json` / `dashboard.html` / `records/mlb,nrl,soccer,tennis` / `stats/upset_patterns.json` + `monitoring/session_61_handoff.md` / `scripts/_session61_*.py` 3本
- **ready_to_implement 候補 2本**: P020 (NRL R014) / P024 (NBA N_NBA_new2)
- **R017 v2.0 / R024 v1.0 / P030 ID衝突解消**: 4本柱完了後の遡及判断タスクで判定
- **Session_61 9件振り分け実施**: 柱D サブセクション9 テーブル参照・4本柱完了後の遡及判断タスクで実施

### 第5段階 (フェーズ6 完了報告 + Session_62 全体総括 + 引継ぎサマリ作成) への引き継ぎ事項

第4段階完了をもって、Session_62 フェーズ6 第5段階に移行可能。第5段階で実施する内容:

1. **フェーズ6 完了報告**: フェーズ6 第1〜4段階の総括 (仮想 Session_63 ワークフロー / Session_61 vs Session_63 比較表 / 議題1〜9 協議 / 議題1/2/3/4/6/7文言/8 実装反映)
2. **Session_62 全体総括**: フェーズ1〜6 の進捗総覧 (4本柱本体実装フェーズ1-5 + 議題確定+反映フェーズ6 + 累計 commit 一覧)
3. **引継ぎサマリ作成**: 4本柱完了状態の確認 + 4本柱完了後の遡及判断タスク一覧整理 + 凍結対象 12件 + ready_to_implement 候補 + 別タスク統合議題 5/7/9 を1ファイルに集約
4. **完了をもって 4本柱 実装作業 完了** → A-3 遡及判断タスクへの移行条件達成

第5段階完了後、本セッションは終了 → 4本柱完了後の遡及判断タスクは別セッションで着手する想定。
